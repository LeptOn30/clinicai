{
    // This file defines custom tasks that can be run in VS Code.
    // For more information, visit: https://go.microsoft.com/fwlink/?LinkId=733558
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "mvn clean install -DskipTests", // For Gradle, use: "gradle build -x test"
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$javac"
            ],
            "detail": "Builds the entire multi-module project, skipping tests."
        }
    ]
}